# Implementation Plans

Generated by the improve skill on 2026-09-22. Execute plans in the order below.
Each executor must read the full plan, honor STOP conditions, and update status
after independent review.

## Execution order and status

| Plan                                      | Title                                          | Priority | Effort | Depends on | Status |
| ----------------------------------------- | ---------------------------------------------- | -------- | ------ | ---------- | ------ |
| [001](001-generalize-knowledge-reader.md) | Generalize the reader for every Knowledge Base | P1       | L      | —          | DONE   |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED` with a reason, or
`REJECTED` with a rationale.

## Dependency notes

- Plan 001 is self-contained.
- Shared, Damian, and Mica manifest updates follow Plan 001 and pin its accepted
  Publisher commit.
- Deployment migration, binary delivery, pnpm migration, search, and PWA work
  remain separate follow-ups.

## Findings considered and rejected

- **Infer navigation only from directories**: rejected because Shared selects
  content hidden by its current navigation and Mica selects an asset pack that
  is not necessarily a primary reader area.
- **Keep a Shared adapter**: rejected because one production adapter per vault
  moves hardcoded rules instead of removing them.
- **Require `index.md` in every selected directory**: rejected because it would
  reorganize canonical Knowledge Bases for a disposable Web Projection.
- **Migrate to pnpm in this plan**: rejected because it changes the root,
  reader, Quartz rollback, Docker, and lockfile contract without helping the
  generic navigation seam.
