name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm
      - name: Install root dependencies
        run: pnpm install --frozen-lockfile
      - name: Check (lint + format)
        run: pnpm run check
      - name: Lint
        run: pnpm run lint
      - name: Focused publisher suites
        run: >-
          pnpm test tests/stage-content.test.mjs
          tests/knowledge-reader-contract.test.mjs tests/navigation.test.mjs
          tests/wiki-aliases.test.mjs tests/wikilink-spike.test.mjs
      - name: Install reader dependencies
        run: pnpm --dir reader install --frozen-lockfile
      - name: Build reader
        run: pnpm --dir reader run build
      - name: Reader static suite (retry once)
        shell: bash
        run: |
          for attempt in 1 2; do
            if pnpm test tests/knowledge-reader-static.test.mjs; then
              break
            elif [ "$attempt" -eq 2 ]; then
              exit 1
            else
              echo "Static suite attempt $attempt failed, retrying..."
            fi
          done
