# Dependencies and build output
node_modules/
content/
# Generated publication metadata (title, canonicalHostname, navigation):
# deterministic build output from staging; never commit it.
site-identity.json

# Knowledge reader (Next.js static export over isolated staged content):
# generated Fumadocs content modules (contain private staged text),
# Next build cache, and disposable static output all stay untracked.
reader/.source/
reader/.next/
reader/out/
reader/next-env.d.ts

# macOS
.DS_Store
